use Taw;

CREATE TABLE Taw.users (
	id INT auto_increment NOT NULL,
	name varchar(100) NULL,
	last_name varchar(100) NULL,
	birt_date varchar(100) NULL,
	directions varchar(100) NULL,
	primary key (id)
)

